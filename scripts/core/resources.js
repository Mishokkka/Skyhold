export const BUILTIN_RESOURCE_DEFS = [
  { id: "money", label: "Деньги", unit: "медн.", icon: "fa-solid fa-coins", category: "Деньги", aliases: ["money", "coin", "coins", "монеты", "деньги", "медь", "медные", "серебро", "золото"], isMoney: true },
  {"id": "black-coal", "label": "Black Coal", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Smith/BlackCoal.png", "category": "Raw Material", "aliases": ["black coal", "уголь", "черный уголь", "blackcoal", "coal", "Black Coal", "raw material Black Coal", "raw material: Black Coal"], "itemUuid": "Compendium.world.resursy.Item.iozYnXMjzPo0PCRd", "itemId": "iozYnXMjzPo0PCRd", "itemType": "rawMaterial", "cost": "5C", "useInProduction": true, "useInConstruction": true},
  {"id": "bread", "label": "Bread", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Bread.png", "category": "Raw Material", "aliases": ["bread", "хлеб", "пища", "еда", "food", "Bread", "raw material Bread", "raw material: Bread"], "itemUuid": "Compendium.world.resursy.Item.lcKETIyM67KGLIzy", "itemId": "lcKETIyM67KGLIzy", "itemType": "rawMaterial", "cost": "4C", "useInProduction": true, "useInConstruction": true},
  {"id": "charcoal", "label": "Charcoal", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Smith/Charcoal.png", "category": "Raw Material", "aliases": ["charcoal", "древесный уголь", "уголь", "Charcoal", "raw material Charcoal", "raw material: Charcoal"], "itemUuid": "Compendium.world.resursy.Item.0Sl72Jyzx8eLBQuR", "itemId": "0Sl72Jyzx8eLBQuR", "itemType": "rawMaterial", "cost": "2C", "useInProduction": true, "useInConstruction": true},
  {"id": "cloth-silk", "label": "Cloth, Silk", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/ClothSilk.png", "category": "Raw Material", "aliases": ["silk cloth", "cloth silk", "шелк", "шелковая ткань", "ткань", "Cloth, Silk", "raw material Cloth, Silk", "raw material: Cloth, Silk"], "itemUuid": "Compendium.world.resursy.Item.5CxK93RSJXmBLZ2p", "itemId": "5CxK93RSJXmBLZ2p", "itemType": "rawMaterial", "cost": "1 G", "useInProduction": true, "useInConstruction": true},
  {"id": "cloth-wool", "label": "Cloth, Wool", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/ClothWool.png", "category": "Raw Material", "aliases": ["wool cloth", "cloth wool", "шерстяная ткань", "сукно", "ткань", "cloth", "Cloth, Wool", "raw material Cloth, Wool", "raw material: Cloth, Wool"], "itemUuid": "Compendium.world.resursy.Item.Qf3zJ6Ike2yuhVwP", "itemId": "Qf3zJ6Ike2yuhVwP", "itemType": "rawMaterial", "cost": "8 C", "useInProduction": true, "useInConstruction": true},
  {"id": "coke", "label": "Coke", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Smith/Charcoal.png", "category": "Raw Material", "aliases": ["coke", "кокс", "Coke", "raw material Coke", "raw material: Coke"], "itemUuid": "Compendium.world.resursy.Item.iQuDQtMclhIi2KJA", "itemId": "iQuDQtMclhIi2KJA", "itemType": "rawMaterial", "cost": "4C", "useInProduction": true, "useInConstruction": true},
  {"id": "copper", "label": "Copper", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/CopperIngot.png", "category": "Raw Material", "aliases": ["copper", "медь", "медный слиток", "Copper", "raw material Copper", "raw material: Copper"], "itemUuid": "Compendium.world.resursy.Item.vfI8j55tsEWUVDrh", "itemId": "vfI8j55tsEWUVDrh", "itemType": "rawMaterial", "cost": "2 S", "useInProduction": true, "useInConstruction": true},
  {"id": "flour", "label": "Flour", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Flour.png", "category": "Raw Material", "aliases": ["flour", "мука", "Flour", "raw material Flour", "raw material: Flour"], "itemUuid": "Compendium.world.resursy.Item.PbQFuw2JzCsc8CeL", "itemId": "PbQFuw2JzCsc8CeL", "itemType": "rawMaterial", "cost": "3C", "useInProduction": true, "useInConstruction": true},
  {"id": "fruits", "label": "Fruits", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Kitchen/Fruits.png", "category": "Raw Material", "aliases": ["fruits", "fruit", "фрукты", "плоды", "пища", "еда", "Fruits", "raw material Fruits", "raw material: Fruits"], "itemUuid": "Compendium.world.resursy.Item.Ypo2tVoRr60eTFRW", "itemId": "Ypo2tVoRr60eTFRW", "itemType": "rawMaterial", "cost": "4C", "useInProduction": true, "useInConstruction": true},
  {"id": "glass", "label": "Glass", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Glass.png", "category": "Raw Material", "aliases": ["glass", "стекло", "Glass", "raw material Glass", "raw material: Glass"], "itemUuid": "Compendium.world.resursy.Item.fWh8rUd5kothZV5B", "itemId": "fWh8rUd5kothZV5B", "itemType": "rawMaterial", "cost": "8 S", "useInProduction": true, "useInConstruction": true},
  {"id": "gold", "label": "Gold", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Gold.png", "category": "Raw Material", "aliases": ["gold", "золото", "золотой слиток", "Gold", "raw material Gold", "raw material: Gold"], "itemUuid": "Compendium.world.resursy.Item.GoI2DCVtPLKpjobj", "itemId": "GoI2DCVtPLKpjobj", "itemType": "rawMaterial", "cost": "10 G", "useInProduction": true, "useInConstruction": true},
  {"id": "grain", "label": "Grain", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Regular/Grain.png", "category": "Raw Material", "aliases": ["grain", "зерно", "злаки", "пища", "еда", "Grain", "raw material Grain", "raw material: Grain"], "itemUuid": "Compendium.world.resursy.Item.6xhdtrjnnRwx6Cab", "itemId": "6xhdtrjnnRwx6Cab", "itemType": "rawMaterial", "cost": "2 C", "useInProduction": true, "useInConstruction": true},
  {"id": "herbs", "label": "Herbs", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Herbs.png", "category": "Raw Material", "aliases": ["herbs", "травы", "трава", "лекарственные травы", "Herbs", "raw material Herbs", "raw material: Herbs"], "itemUuid": "Compendium.world.resursy.Item.SupoqycbuiyAHUL1", "itemId": "SupoqycbuiyAHUL1", "itemType": "rawMaterial", "cost": "2 S", "useInProduction": true, "useInConstruction": true},
  {"id": "honey", "label": "Honey", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Kitchen/Honey.png", "category": "Raw Material", "aliases": ["honey", "мед", "мёд", "пища", "еда", "Honey", "raw material Honey", "raw material: Honey"], "itemUuid": "Compendium.world.resursy.Item.XLmcnqeYFQXlbaJQ", "itemId": "XLmcnqeYFQXlbaJQ", "itemType": "rawMaterial", "cost": "40C", "useInProduction": true, "useInConstruction": true},
  {"id": "iron", "label": "Iron", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Iron.png", "category": "Raw Material", "aliases": ["iron", "железо", "металл", "слитки", "metal", "Iron", "raw material Iron", "raw material: Iron"], "itemUuid": "Compendium.world.resursy.Item.CQpPmttjTUiT3NKM", "itemId": "CQpPmttjTUiT3NKM", "itemType": "rawMaterial", "cost": "1 S", "useInProduction": true, "useInConstruction": true},
  {"id": "iron-ore", "label": "Iron Ore", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/IronOre.png", "category": "Raw Material", "aliases": ["iron ore", "ore", "руда", "железная руда", "Iron Ore", "raw material Iron Ore", "raw material: Iron Ore"], "itemUuid": "Compendium.world.resursy.Item.0gwozD4R4pn9Got9", "itemId": "0gwozD4R4pn9Got9", "itemType": "rawMaterial", "cost": "4 C", "useInProduction": true, "useInConstruction": true},
  {"id": "leather", "label": "Leather", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Leather.png", "category": "Raw Material", "aliases": ["leather", "кожа", "Leather", "raw material Leather", "raw material: Leather"], "itemUuid": "Compendium.world.resursy.Item.5E4IQjsvA1sJ8Fns", "itemId": "5E4IQjsvA1sJ8Fns", "itemType": "rawMaterial", "cost": "12 C", "useInProduction": true, "useInConstruction": true},
  {"id": "meat", "label": "Meat", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Kitchen/Meat.png", "category": "Raw Material", "aliases": ["meat", "мясо", "пища", "еда", "food", "Meat", "raw material Meat", "raw material: Meat"], "itemUuid": "Compendium.world.resursy.Item.wwCjXxxnTd2YpJ0s", "itemId": "wwCjXxxnTd2YpJ0s", "itemType": "rawMaterial", "cost": "6C", "useInProduction": true, "useInConstruction": true},
  {"id": "milk", "label": "Milk", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Kitchen/Milk.png", "category": "Raw Material", "aliases": ["milk", "молоко", "пища", "еда", "Milk", "raw material Milk", "raw material: Milk"], "itemUuid": "Compendium.world.resursy.Item.lTu15CSlUJ6nGjZS", "itemId": "lTu15CSlUJ6nGjZS", "itemType": "rawMaterial", "cost": "3C", "useInProduction": true, "useInConstruction": true},
  {"id": "pelt", "label": "Pelt", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Pelt.png", "category": "Raw Material", "aliases": ["pelt", "шкура", "шкуры", "мех", "Pelt", "raw material Pelt", "raw material: Pelt"], "itemUuid": "Compendium.world.resursy.Item.B85XQAx3hVinQbLq", "itemId": "B85XQAx3hVinQbLq", "itemType": "rawMaterial", "cost": "8 C", "useInProduction": true, "useInConstruction": true},
  {"id": "silver", "label": "Silver", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Silver.png", "category": "Raw Material", "aliases": ["silver", "серебро", "серебряный слиток", "Silver", "raw material Silver", "raw material: Silver"], "itemUuid": "Compendium.world.resursy.Item.cjJeJAW8O7A3JlRw", "itemId": "cjJeJAW8O7A3JlRw", "itemType": "rawMaterial", "cost": "1 G", "useInProduction": true, "useInConstruction": true},
  {"id": "stone", "label": "Stone", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Stone.png", "category": "Raw Material", "aliases": ["stone", "камень", "камни", "валун", "каменья", "Stone", "raw material Stone", "raw material: Stone"], "itemUuid": "Compendium.world.resursy.Item.aKdiGkyLV3sUQIRr", "itemId": "aKdiGkyLV3sUQIRr", "itemType": "rawMaterial", "cost": "2 C", "useInProduction": true, "useInConstruction": true},
  {"id": "tallow", "label": "Tallow", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Tallow.png", "category": "Raw Material", "aliases": ["tallow", "жир", "сало", "Tallow", "raw material Tallow", "raw material: Tallow"], "itemUuid": "Compendium.world.resursy.Item.x4b1cSnctu3G3yBD", "itemId": "x4b1cSnctu3G3yBD", "itemType": "rawMaterial", "cost": "6 C", "useInProduction": true, "useInConstruction": true},
  {"id": "vegetables", "label": "Vegetables", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Kitchen/Veggie.png", "category": "Raw Material", "aliases": ["vegetables", "veggie", "овощи", "овощ", "пища", "еда", "Vegetables", "raw material Vegetables", "raw material: Vegetables"], "itemUuid": "Compendium.world.resursy.Item.Vi0DphgtkOLLFm6c", "itemId": "Vi0DphgtkOLLFm6c", "itemType": "rawMaterial", "cost": "4C", "useInProduction": true, "useInConstruction": true},
  {"id": "wood", "label": "Wood", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/Regular/Wood.png", "category": "Raw Material", "aliases": ["wood", "дерево", "древесина", "лес", "бревна", "доски", "Wood", "raw material Wood", "raw material: Wood"], "itemUuid": "Compendium.world.resursy.Item.zSA7X1QooILVS69A", "itemId": "zSA7X1QooILVS69A", "itemType": "rawMaterial", "cost": "3 C", "useInProduction": true, "useInConstruction": true},
  {"id": "wool", "label": "Wool", "unit": "ед.", "icon": "fa-solid fa-cube", "img": "3_F00L/Icons/Items/TradeGoods/Wool.png", "category": "Raw Material", "aliases": ["wool", "шерсть", "Wool", "raw material Wool", "raw material: Wool"], "itemUuid": "Compendium.world.resursy.Item.0jaPgPUC0NcJjT66", "itemId": "0jaPgPUC0NcJjT66", "itemType": "rawMaterial", "cost": "4 C", "useInProduction": true, "useInConstruction": true},
  {"id": "water", "label": "Water", "unit": "ед.", "icon": "fa-solid fa-droplet", "category": "Legacy", "aliases": ["water", "вода"], "useInProduction": true, "useInConstruction": false, "notes": "Служебный fallback: в присланном rawMaterial-компендиуме воды нет."}
];

// Совместимость: старое имя экспорта оставлено, но функции ниже используют
// не только встроенные ресурсы, а еще и ГМ-каталог из world setting.
export const RESOURCE_DEFS = BUILTIN_RESOURCE_DEFS;

function clonePlain(value) {
  return JSON.parse(JSON.stringify(value));
}

export function normalizeKey(value) {
  let text = String(value ?? "")
    .trim()
    .toLowerCase()
    .replace(/ё/g, "е")
    .replace(/[«»\"']/g, "")
    .replace(/\s+/g, " ");

  // Раньше неизвестные ресурсы могли сохраняться как "Другое: Древесина".
  // При распознавании убираем служебные префиксы, чтобы такие строки сливались
  // обратно с каноническими ресурсами.
  text = text
    .replace(/^(другое|custom|прочее)\s*[:：-]\s*/i, "")
    .replace(/^(raw\s*material|сырье|материал)\s*[:：-]\s*/i, "")
    .replace(/[\s_\-]+/g, " ")
    .trim();
  return text;
}

export function normalizeResourceCatalogRow(row = {}) {
  const label = String(row.label ?? row.name ?? "Новый ресурс").trim() || "Новый ресурс";
  const rawId = String(row.id ?? "").trim();
  const id = safeResourceId(rawId || label);
  const aliases = Array.isArray(row.aliases)
    ? row.aliases.map((item) => String(item ?? "").trim()).filter(Boolean)
    : String(row.aliases ?? "").split(/[,;\n]/).map((item) => item.trim()).filter(Boolean);

  return {
    id,
    label,
    unit: String(row.unit ?? "ед.").trim() || "ед.",
    icon: String(row.icon ?? "fa-solid fa-cube").trim() || "fa-solid fa-cube",
    img: String(row.img ?? row.image ?? "").trim(),
    category: String(row.category ?? "Пользовательское").trim() || "Пользовательское",
    aliases,
    itemUuid: String(row.itemUuid ?? "").trim(),
    itemId: String(row.itemId ?? "").trim(),
    itemType: String(row.itemType ?? "").trim(),
    cost: String(row.cost ?? "").trim(),
    shelfLife: String(row.shelfLife ?? "").trim(),
    useInProduction: row.useInProduction !== false,
    useInConstruction: row.useInConstruction !== false,
    isMoney: Boolean(row.isMoney),
    hidden: Boolean(row.hidden),
    notes: String(row.notes ?? "")
  };
}

function safeResourceId(value = "") {
  const latin = String(value ?? "")
    .trim()
    .toLowerCase()
    .replace(/ё/g, "е")
    .replace(/[^a-z0-9а-я]+/gi, "-")
    .replace(/^-+|-+$/g, "");
  const translit = transliterate(latin).replace(/[^a-z0-9-]+/g, "").replace(/^-+|-+$/g, "");
  return translit || `resource-${Date.now()}`;
}

function transliterate(value = "") {
  const map = {
    а: "a", б: "b", в: "v", г: "g", д: "d", е: "e", ж: "zh", з: "z", и: "i", й: "y", к: "k", л: "l", м: "m",
    н: "n", о: "o", п: "p", р: "r", с: "s", т: "t", у: "u", ф: "f", х: "h", ц: "ts", ч: "ch", ш: "sh",
    щ: "sch", ъ: "", ы: "y", ь: "", э: "e", ю: "yu", я: "ya"
  };
  return String(value ?? "").replace(/[а-я]/g, (char) => map[char] ?? char);
}

export function getResourceCatalogRows() {
  try {
    const catalog = globalThis.game?.settings?.get?.("fbl-skyhold", "skyholdCatalog");
    const legacy = globalThis.game?.settings?.get?.("fbl-skyhold", "skyholdData");
    const rows = Array.isArray(catalog?.resources) ? catalog.resources : legacy?.catalog?.resources;
    if (!Array.isArray(rows)) return [];
    return rows.map((row) => normalizeResourceCatalogRow(row)).filter((row) => !row.hidden);
  } catch (_error) {
    return [];
  }
}

export function allResourceDefs({ includeHidden = false } = {}) {
  const map = new Map();
  for (const resource of BUILTIN_RESOURCE_DEFS) map.set(resource.id, clonePlain(resource));
  for (const resource of getResourceCatalogRows()) {
    if (resource.hidden && !includeHidden) continue;
    map.set(resource.id, { ...resource });
  }
  return Array.from(map.values());
}

function resourceMap() {
  return new Map(allResourceDefs().map((resource) => [resource.id, resource]));
}

function aliasMap() {
  const map = new Map();
  for (const resource of allResourceDefs()) {
    map.set(normalizeKey(resource.id), resource.id);
    map.set(normalizeKey(resource.label), resource.id);
    for (const alias of resource.aliases ?? []) map.set(normalizeKey(alias), resource.id);
  }
  return map;
}

export function normalizeResourceId(...candidates) {
  const values = candidates.length ? candidates : [""];
  const resources = resourceMap();
  const aliases = aliasMap();

  // Сначала пробуем все кандидаты, кроме явного custom. Это важно для строк вида
  // { resourceId: "custom", resource: "Древесина" } из старых версий.
  for (const candidate of values) {
    const raw = String(candidate ?? "").trim();
    if (!raw || normalizeKey(raw) === "custom" || normalizeKey(raw) === "другое") continue;
    if (resources.has(raw)) return raw;
    const found = aliases.get(normalizeKey(raw));
    if (found) return found;
  }

  // Если ничего не нашли и был явный custom, оставляем custom. Иначе тоже custom.
  return "custom";
}

export function resourceDef(...candidates) {
  const key = normalizeResourceId(...candidates);
  return resourceMap().get(key) ?? null;
}

export function resourceLabel(resource = "") {
  if (resource && typeof resource === "object") {
    const id = normalizeResourceId(resource.id, resource.resourceId, resource.name, resource.label);
    const def = id !== "custom" ? resourceMap().get(id) : null;
    return def?.label ?? String(resource.name ?? resource.label ?? "Ресурс");
  }
  const id = normalizeResourceId(resource);
  return resourceMap().get(id)?.label ?? String(resource || "Ресурс").replace(/^(Другое|Custom)\s*[:：-]\s*/i, "");
}

export function resourceUnit(...candidates) {
  const def = resourceDef(...candidates);
  return def?.unit ?? "ед.";
}

export function resourceIcon(...candidates) {
  const def = resourceDef(...candidates);
  return def?.icon ?? "fa-solid fa-cube";
}

export function resourceImage(...candidates) {
  const def = resourceDef(...candidates);
  return def?.img ?? "";
}

export function resourceItemUuid(...candidates) {
  const def = resourceDef(...candidates);
  return def?.itemUuid ?? "";
}

export function isMoneyResource(...candidates) {
  const id = normalizeResourceId(...candidates);
  if (id === "money") return true;
  const def = id !== "custom" ? resourceMap().get(id) : null;
  if (def?.isMoney) return true;
  // Если строка распознана как конкретный rawMaterial (Gold/Silver), не превращаем ее в казну.
  if (def && id !== "custom") return false;
  return candidates.some((candidate) => /(деньг|монет|money|coin|coins|медн|серебр|золот)/i.test(String(candidate ?? "")));
}

export function resourceOptions(currentId = "", currentName = "", { includeMoney = true } = {}) {
  const selectedId = normalizeResourceId(currentId, currentName);
  const options = allResourceDefs()
    .filter((resource) => includeMoney || !isMoneyResource(resource.id, resource.label))
    .map((resource) => ({
      value: resource.id,
      label: resource.label,
      selected: selectedId === resource.id
    }));
  const customName = String(currentName ?? "").trim().replace(/^(Другое|Custom)\s*[:：-]\s*/i, "");
  options.push({ value: "custom", label: customName ? `Другое: ${customName}` : "Другое", selected: selectedId === "custom" });
  return options;
}

export function normalizeResourceRow(row = {}) {
  const id = normalizeResourceId(row.resourceId, row.id, row.name, row.label);
  const def = id !== "custom" ? resourceMap().get(id) : null;
  const name = String(row.name ?? row.label ?? def?.label ?? "Ресурс").replace(/^(Другое|Custom)\s*[:：-]\s*/i, "");
  return {
    id: String(row.rowId ?? row._rowId ?? row.uuid ?? row._id ?? row.key ?? row.id ?? globalThis.foundry?.utils?.randomID?.(12) ?? `res-${Date.now()}`),
    resourceId: id,
    name: id === "custom" ? name : def.label,
    img: String(row.img ?? def?.img ?? ""),
    itemUuid: String(row.itemUuid ?? def?.itemUuid ?? ""),
    itemId: String(row.itemId ?? def?.itemId ?? ""),
    itemType: String(row.itemType ?? def?.itemType ?? ""),
    cost: String(row.cost ?? def?.cost ?? ""),
    qty: Number(row.qty ?? row.quantity ?? row.count ?? 0) || 0,
    unit: String(row.unit ?? def?.unit ?? "ед."),
    notes: String(row.notes ?? ""),
    roomId: String(row.roomId ?? row.storageRoomId ?? "outdoors"),
    forSale: Boolean(row.forSale ?? row.sale ?? row.markedForSale ?? false)
  };
}


function normalizeProductionSeasons(value = null) {
  const defs = ["spring", "summer", "autumn", "winter"];
  const fallback = Object.fromEntries(defs.map((key) => [key, true]));
  if (!value || typeof value !== "object" || Array.isArray(value)) return fallback;
  const normalized = {};
  let enabled = 0;
  for (const key of defs) {
    const active = value[key] !== false;
    normalized[key] = Boolean(active);
    if (active) enabled += 1;
  }
  return enabled ? normalized : fallback;
}

export function normalizeProductionLine(line = {}, makeId = null) {
  const id = String(line?.id || makeId?.("prod") || globalThis.foundry?.utils?.randomID?.(12) || `prod-${Date.now()}`);
  const resourceId = normalizeResourceId(line?.resourceId, line?.resource, line?.name);
  const def = resourceId !== "custom" ? resourceMap().get(resourceId) : null;
  const resource = String(line?.resource ?? line?.name ?? def?.label ?? "Ресурс").replace(/^(Другое|Custom)\s*[:：-]\s*/i, "");
  const legacyBase = Number(line?.base ?? 0) || 0;
  const legacyPerWorker = Number(line?.perWorker ?? 0) || 0;
  const rawSource = String(line?.source ?? line?.sourceType ?? line?.productionSource ?? "workers").trim().toLowerCase();
  const source = ["workers", "time", "content"].includes(rawSource) ? rawSource : "workers";
  const rawPeriod = String(line?.period ?? line?.periodType ?? "day").trim().toLowerCase();
  const period = ["qd", "day", "tenday"].includes(rawPeriod) ? rawPeriod : "day";
  const outputQty = Math.max(0, Number((line?.outputQty ?? line?.output ?? line?.quantity ?? legacyBase) || legacyPerWorker || 1) || 1);
  const workQd = Math.max(1, Number(line?.workQd ?? line?.workerQD ?? line?.cycleQd ?? line?.cycleQD ?? 1) || 1);
  const rawExpenseMode = String(line?.expenseMode ?? line?.costMode ?? "cycle").trim().toLowerCase();
  const expenseMode = ["cycle", "unit"].includes(rawExpenseMode) ? rawExpenseMode : "cycle";
  const rawAutoCollect = String(line?.autoCollect ?? line?.collectionMode ?? "none").trim().toLowerCase();
  const autoCollect = ["none", "building", "worker"].includes(rawAutoCollect) ? rawAutoCollect : "none";

  return {
    id,
    active: line?.active !== false,
    mode: String(line?.mode ?? "Всегда"),
    source,
    period,
    contentId: String(line?.contentId ?? line?.contentKey ?? line?.content ?? ""),
    resourceId,
    resource: resourceId === "custom" ? resource : def.label,
    // Совместимость со старой схемой. Эти поля пока сохраняются,
    // но новая экономика считает производство через рабочие QD и циклы.
    base: legacyBase,
    perWorker: legacyPerWorker,
    outputQty,
    workQd,
    formula: String(line?.formula ?? ""),
    expenses: String(line?.expenses ?? ""),
    expenseMode,
    requiresCollection: Boolean(line?.requiresCollection ?? line?.collect ?? false),
    collectQd: Math.max(0, Number(line?.collectQd ?? line?.collectionQd ?? 0) || 0),
    autoCollect,
    overtime: Boolean(line?.overtime ?? line?.overwork ?? false),
    pendingQty: Math.max(0, Number(line?.pendingQty ?? line?.storedQty ?? line?.readyQty ?? 0) || 0),
    storageRoomId: String(line?.storageRoomId ?? line?.roomId ?? "outdoors"),
    seasons: normalizeProductionSeasons(line?.seasons),
    itemUuid: String(line?.itemUuid ?? def?.itemUuid ?? ""),
    itemId: String(line?.itemId ?? def?.itemId ?? "")
  };
}

export function storageResourceKey(resource = {}) {
  const id = normalizeResourceId(resource.resourceId, resource.id, resource.name);
  const roomId = String(resource.roomId ?? resource.storageRoomId ?? "outdoors") || "outdoors";
  const resourceKey = id !== "custom" ? `id:${id}` : `custom:${normalizeKey(resource.name)}`;
  return `room:${roomId}|${resourceKey}`;
}
